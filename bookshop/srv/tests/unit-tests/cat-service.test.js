let {bookValue} = require('../../cat-service')
describe('test', () => {
    it('bookValue', async () => {
        // given


        // when
        const result = bookValue()

        // then

    })
})